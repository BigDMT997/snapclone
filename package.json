{
  "name": "snapclone",
  "version": "1.0.0",
  "description": "Snapchat PWA Clone",
  "scripts": {
    "start": "cd server && npm install && node server.js",
    "build": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}